import React from 'react';

const EditPlantInformation = () => {
    return (
        <>
            Edit Plant Information Page
            {/*
            pulls in only the plant that was chosen to edit from state,
            has editable fields for plant properties,
            has save button => saves changes to state => redirects user back
             to list,
            has a logout button
            */}
        </>
    )
}

export default EditPlantInformation;
