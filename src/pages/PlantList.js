import React from 'react';

const PlantList = () => {
    return(
        <>
            Plant List Page
        {/*
        pulls in the list of plants the user has,
        displays the plants,
        has edit button => redirects to an edit plants page,
        has a logout button => redirects to home page
        */}
        </>
    )
}

export default PlantList;
